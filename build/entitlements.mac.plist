<?xml version="1.0" encoding="UTF-8"?>
<!--
  Entitlements for the signed macOS build. We allow JIT (needed by Chromium),
  unsigned executable memory (needed by native node modules), and Apple Events
  (so the contextReader's AppleScript calls work). The user must additionally
  grant the app Accessibility permission in System Settings → Privacy & Security
  for keyboard simulation in autoFill.js to function.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.automation.apple-events</key>
    <true/>
  </dict>
</plist>
